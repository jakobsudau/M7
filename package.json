{
    "name": "m7",
    "productName": "m7",
    "version": "0.0.1",
    "license": "MIT",
    "author": "Jakob Sudau",
    "main": "main.js",
    "scripts": {
        "installProject": "cd libs/magenta-js/music && yarn install && yarn build && cd ../../../ && cd server/ && yarn install && cd ../ && yarn install",
        "startElectron": "electron .",
        "startNode": "cd ./server && npm run start --scripts-prepend-node-path",
        "buildElectron": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=electron/icon/icon.icns --prune=true --out=electron/release-builds --ignore=server --ignore=libs --ignore=assets"
    },
    "dependencies": {
        "@magenta/music": "libs/magenta-js/music"
    },
    "devDependencies": {
        "electron": "^5.0.3",
        "electron-packager": "^13.1.1"
    }
}
